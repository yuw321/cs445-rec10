package cs445.rec10;

import java.util.Iterator;

public class IterableUtils {

    /**
     * prints a list of enteries (of any type: T),
     * using the traversal behaviour 
     * of the Iterator pattern
     * @param collection: the list to be printed
     */
    public static <T> void printList(Iterable<T> collection) {
        // TODO: implement this method
    }

    /**
     * removes all the enteries (of type: String),
     * whose length is less than a certain limit
     * @param collection: the list to be printed
     * @param limit: the upper limit for the length of strings to be removed 
     */
    public static void removeShortStrings(Iterable<String> collection, int limit) {
        // TODO: implement this method
    }

    /**
     * find the most repetitive entry (Mode),
     * in a list of enteries (of any type: T),
     * @param collection: the list to be printed
     * @return T: the mode
     */
    public static <T> T findMode(Iterable<T> collection) {
        // TODO: implement this method
        return null;
    }
}

